import { SharedSortableDirective } from './shared-sortable.directive';

describe('SharedSortableDirective', () => {
  it('should create an instance', () => {
    const directive = new SharedSortableDirective();
    expect(directive).toBeTruthy();
  });
});
